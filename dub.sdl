name "udt_d"
description "Wrapper for UDT - reliable UDP protocol"
authors "Laeeth Isharc"
copyright "Copyright © 2018, Laeeth Isharc and Kaleidic Associates"
license "boost"
targetType "library"
targetName "udtwrap"
# There used to be a command here to run d++ but only works with a custom-build one.
# For unknown reasons `dub run dpp` doesn't seem to work unless ran manually on the
# command line. Since the .d file is checked in anyway, we leave it to the maintainer
# update it as needed.
